from setuptools import setup

setup(
    name = "aitoolkit",
    version = "0.0.2",
    install_requires=[
            'numpy',
            'tensorflow',
            'keras',
            ],
    packages=['aitoolkit'],
)
